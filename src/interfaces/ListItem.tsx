export interface ListItem {
  title: string;
  idx:number;
  isFavorite: boolean;
  created:string;
  edited:string;
  //the abstract
  opening_crawl:string;
  director:string;
  producer:string;
  release_date:string;
  };